const ageRange = require('./ageRange')
const children = require('./children')
const city = require('./city')
const enrollList = require('./enrollList')
const loginHistory = require('./loginHistory')
const playHistory = require('./playHistory')
const province = require('./province')
const staredCollection = require('./staredCollection')
const user = require('./user')
const video = require('./video')
const videoCategory = require('./videoCategory')
const videoCategoryRef = require('./videoCategoryRef')
const videoList = require('./videoList')
const videoListRef = require('./videoListRef')
const videoListSuitable = require('./videoListSuitable')
const videoTag = require('./videoTag')
const videoTagRef = require('./videoTagRef')


module.exports = {
  ageRange : ageRange,
  children : children,
  city : city,
  enrollList : enrollList,
  loginHistory : loginHistory,
  playHistory : playHistory,
  province : province,
  staredCollection : staredCollection,
  user : user,
  video : video,
  videoCategory : videoCategory,
  videoCategoryRef : videoCategoryRef,
  videoList : videoList,
  videoListRef : videoListRef,
  videoListSuitable : videoListSuitable,
  videoTag : videoTag,
  videoTagRef : videoTagRef,
}
